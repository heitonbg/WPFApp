<Window x:Class="WpfApp1.GoldenRatioWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="Метод золотого сечения"
        Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing"
        Loaded="Window_Loaded"
        FontFamily="Segoe UI">

	<Window.Resources>
		<Style x:Key="ModernButton" TargetType="Button">
			<Setter Property="Background" Value="#2C5F9E"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Height" Value="32"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}">
							<ContentPresenter x:Name="contentPresenter"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            Content="{TemplateBinding Content}"
                                            ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="10,5"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="#3A6DB0"/>
								<Setter TargetName="border" Property="Effect">
									<Setter.Value>
										<DropShadowEffect BlurRadius="8" ShadowDepth="0" Color="#30000000"/>
									</Setter.Value>
								</Setter>
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="border" Property="Background" Value="#1E4A7A"/>
								<Setter TargetName="border" Property="RenderTransform">
									<Setter.Value>
										<ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
									</Setter.Value>
								</Setter>
							</Trigger>
							<Trigger Property="IsEnabled" Value="False">
								<Setter TargetName="border" Property="Background" Value="#B0B0B0"/>
								<Setter TargetName="border" Property="Opacity" Value="0.6"/>
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<Style x:Key="GroupBoxStyle" TargetType="GroupBox">
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="BorderBrush" Value="#2C5F9E"/>
			<Setter Property="Background" Value="#FFFFFF"/>
			<Setter Property="Foreground" Value="#2C5F9E"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Padding" Value="10"/>
			<Setter Property="Margin" Value="10"/>
		</Style>

		<Style x:Key="TextBoxStyle" TargetType="TextBox">
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="BorderBrush" Value="#2C5F9E"/>
			<Setter Property="Background" Value="#FFFFFF"/>
			<Setter Property="Foreground" Value="#2C5F9E"/>
			<Setter Property="Padding" Value="8,4"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="SelectionBrush" Value="#2C5F9E40"/>
		</Style>

		<Style x:Key="LabelStyle" TargetType="Label">
			<Setter Property="Foreground" Value="#2C5F9E"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="FontSize" Value="13"/>
		</Style>

		<Style x:Key="ComboBoxStyle" TargetType="ComboBox">
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="BorderBrush" Value="#2C5F9E"/>
			<Setter Property="Background" Value="#FFFFFF"/>
			<Setter Property="Foreground" Value="#2C5F9E"/>
			<Setter Property="Padding" Value="8,4"/>
			<Setter Property="FontSize" Value="13"/>
			<Setter Property="Height" Value="32"/>
		</Style>

		<Style x:Key="MenuStyle" TargetType="Menu">
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="Foreground" Value="#2C5F9E"/>
		</Style>

		<Style x:Key="MenuItemStyle" TargetType="MenuItem">
			<Setter Property="Foreground" Value="#2C5F9E"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="Background" Value="White"/>
		</Style>
	</Window.Resources>

	<Window.Background>
		<LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
			<GradientStop Color="#f0f5fc" Offset="0.0" />
			<GradientStop Color="#e8eff8" Offset="0.4" />
			<GradientStop Color="#dde6f2" Offset="1.0" />
		</LinearGradientBrush>
	</Window.Background>

	<Grid>
		<Ellipse Width="200" Height="200" Opacity="0.04" Fill="#2C5F9E" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-60,-60"/>
		<Ellipse Width="150" Height="150" Opacity="0.04" Fill="#2C5F9E" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="-40,-40"/>

		<Grid Margin="20">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<!-- Menu -->
			<Menu Grid.Row="0" Background="Transparent" Style="{StaticResource MenuStyle}">
				<MenuItem Header="Файл" Style="{StaticResource MenuItemStyle}">
					<MenuItem Header="Найти экстремум" Click="Calculate_Click" Style="{StaticResource MenuItemStyle}"/>
					<MenuItem Header="Очистить" Click="Clear_Click" Style="{StaticResource MenuItemStyle}"/>
					<Separator/>
					<MenuItem Header="Выход" Click="Exit_Click" Style="{StaticResource MenuItemStyle}"/>
				</MenuItem>
			</Menu>

			<!-- Header -->
			<StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,15">
				<TextBlock Text="Метод золотого сечения"
                          FontSize="28"
                          FontWeight="Light"
                          Foreground="#2C5F9E"
                          HorizontalAlignment="Center"/>
				<TextBlock Text="Поиск экстремума функции"
                          FontSize="14"
                          FontWeight="Normal"
                          Foreground="#2C5F9E"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
			</StackPanel>

			<!-- Input parameters -->
			<GroupBox Grid.Row="2" Header="Параметры расчета" Style="{StaticResource GroupBoxStyle}" Margin="0,0,0,10">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto"/>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>

					<Label Grid.Row="0" Grid.Column="0" Content="Тип экстремума:" Style="{StaticResource LabelStyle}" Margin="5"/>
					<ComboBox Grid.Row="0" Grid.Column="1" Name="cmbExtremumType" Style="{StaticResource ComboBoxStyle}">
						<ComboBoxItem Content="Минимум" Foreground="#2C5F9E"/>
						<ComboBoxItem Content="Максимум" Foreground="#2C5F9E"/>
					</ComboBox>

					<Label Grid.Row="1" Grid.Column="0" Content="Функция f(x):" Style="{StaticResource LabelStyle}" Margin="5"/>
					<TextBox Grid.Row="1" Grid.Column="1" Name="txtFunction" Style="{StaticResource TextBoxStyle}"
                             Text="pow(x,2)" ToolTip="Введите математическое выражение функции. Примеры: pow(x,2), sin(x), exp(x)"/>

					<Label Grid.Row="2" Grid.Column="0" Content="Начало интервала a:" Style="{StaticResource LabelStyle}" Margin="5"/>
					<TextBox Grid.Row="2" Grid.Column="1" Name="txtA" Style="{StaticResource TextBoxStyle}" Text="-2"/>

					<Label Grid.Row="3" Grid.Column="0" Content="Конец интервала b:" Style="{StaticResource LabelStyle}" Margin="5"/>
					<TextBox Grid.Row="3" Grid.Column="1" Name="txtB" Style="{StaticResource TextBoxStyle}" Text="2"/>

					<Label Grid.Row="4" Grid.Column="0" Content="Точность ε:" Style="{StaticResource LabelStyle}" Margin="5"/>
					<TextBox Grid.Row="4" Grid.Column="1" Name="txtEpsilon" Style="{StaticResource TextBoxStyle}" Text="0,001"/>

					<Button Grid.Row="5" Grid.Column="1" Name="btnCalculate" Content="Найти экстремум"
                            Style="{StaticResource ModernButton}" HorizontalAlignment="Right" Width="140" Click="Calculate_Click"/>
				</Grid>
			</GroupBox>

			<!-- Results and Chart -->
			<GroupBox Grid.Row="3" Header="Результаты расчета" Style="{StaticResource GroupBoxStyle}">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="*"/>
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="250"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>

					<!-- Results -->
					<StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Margin="0,0,15,0">
						<TextBlock Name="lblResult" Text="Точка экстремума: " FontWeight="Bold" Foreground="#2C5F9E" Margin="0,0,0,8" FontSize="14"/>
					</StackPanel>

					<!-- Chart -->
					<Border Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" BorderThickness="1" BorderBrush="#2C5F9E"
                            Background="#FFFFFF" CornerRadius="4" Padding="10" Margin="0,0,0,0"
                            Height="300" VerticalAlignment="Stretch">
						<oxy:PlotView Name="plotView" Model="{Binding PlotModel}"
                                    Height="280"
                                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
					</Border>
				</Grid>
			</GroupBox>
		</Grid>
	</Grid>
</Window>
